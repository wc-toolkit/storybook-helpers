---
"@wc-toolkit/storybook-helpers": minor
---

Add `scopedStylesDecorator` to allow scoping CSS custom properties to a story by wrapping the story in a slugified class. Also includes small rendering/formatting fixes to ensure Storybook Docs show nicely indented markup.
